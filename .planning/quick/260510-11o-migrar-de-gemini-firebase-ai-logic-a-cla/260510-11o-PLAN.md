---
phase: 260510-11o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/gradle/libs.versions.toml
  - android/build.gradle.kts
  - android/app/build.gradle.kts
  - android/app/google-services.json
  - android/app/src/main/java/com/beto/app/llm/AnthropicClientHolder.kt
  - android/app/src/main/java/com/beto/app/llm/GeminiLlmClient.kt
  - android/app/src/main/java/com/beto/app/llm/ClaudeLlmClient.kt
  - android/app/src/main/java/com/beto/app/companion/CompanionLlmClient.kt
  - android/app/src/main/java/com/beto/app/BetoApplication.kt
  - android/app/src/main/java/com/beto/app/voice/VoiceCaptureActivity.kt
  - android/app/src/main/java/com/beto/app/service/BetoForegroundService.kt
autonomous: true
requirements:
  - QUICK-260510-11o
must_haves:
  truths:
    - "El APK compila sin dependencias de Firebase (gradle build pasa)."
    - "El motor de acciones interpreta comandos via Claude Haiku 4.5 (no Gemini)."
    - "El Modo Compañero responde y extrae facts via Claude Haiku 4.5."
    - "La API key de Anthropic se carga desde local.properties via BuildConfig (no commiteada)."
  artifacts:
    - path: "android/app/src/main/java/com/beto/app/llm/AnthropicClientHolder.kt"
      provides: "Singleton AnthropicOkHttpClient + helper suspend para llamadas con Dispatchers.IO."
    - path: "android/app/src/main/java/com/beto/app/llm/ClaudeLlmClient.kt"
      provides: "Implementación de LlmClient + TranscriptCorrectionClient sobre claude-haiku-4-5."
    - path: "android/app/src/main/java/com/beto/app/companion/CompanionLlmClient.kt"
      provides: "Chat + extracción JSON de facts sobre claude-haiku-4-5."
  key_links:
    - from: "android/app/src/main/java/com/beto/app/BetoApplication.kt"
      to: "ClaudeLlmClient"
      via: "PhraseGenerator(ClaudeLlmClient())"
      pattern: "ClaudeLlmClient\\("
    - from: "android/app/src/main/java/com/beto/app/service/BetoForegroundService.kt"
      to: "ClaudeLlmClient"
      via: "llm = ClaudeLlmClient()"
      pattern: "llm = ClaudeLlmClient\\("
    - from: "android/app/src/main/java/com/beto/app/voice/VoiceCaptureActivity.kt"
      to: "ClaudeLlmClient"
      via: "SttCorrector(ClaudeLlmClient())"
      pattern: "SttCorrector\\(ClaudeLlmClient"
    - from: "android/app/src/main/java/com/beto/app/llm/ClaudeLlmClient.kt"
      to: "AnthropicClientHolder"
      via: "AnthropicClientHolder.complete(...)"
      pattern: "AnthropicClientHolder\\."
    - from: "android/app/src/main/java/com/beto/app/companion/CompanionLlmClient.kt"
      to: "AnthropicClientHolder"
      via: "AnthropicClientHolder.complete(...)"
      pattern: "AnthropicClientHolder\\."
---

<objective>
Migrar el cliente LLM de Gemini (Firebase AI Logic) a Claude Haiku 4.5 usando el SDK oficial `anthropic-java` 2.30.0. Reemplazar `GeminiLlmClient` por un nuevo `ClaudeLlmClient` y reescribir los `defaultGenerator`s del Compañero, manteniendo intactas las interfaces (`LlmClient`, `TranscriptCorrectionClient`, `CompanionChatClient`) y los prompts (`PromptBuilder`).

Purpose: Anthropic SDK trae mejor reputación en agentic tool calling (relevante para el motor de acciones) y elimina la dependencia de Firebase project / google-services.json. Trade-off aceptado: API key embebida via BuildConfig — explícitamente OK en CLAUDE.md "Stack Patterns by Variant" para demo single-device.

Output: APK que compila sin Firebase, ambos clientes LLM ruteados a `claude-haiku-4-5`, key cargada desde `local.properties`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md

<interfaces>
<!-- Contracts that MUST stay stable. Do NOT change these signatures. -->

From android/app/src/main/java/com/beto/app/llm/LlmClient.kt:
```kotlin
interface LlmClient {
    suspend fun interpret(rawTranscript: String): Decision
    suspend fun generatePhrase(prompt: String): String = ""
}
```

From android/app/src/main/java/com/beto/app/voice/TranscriptCorrectionClient.kt (existing — used by SttCorrector):
```kotlin
interface TranscriptCorrectionClient {
    suspend fun correctTranscript(prompt: String): String
}
```

From android/app/src/main/java/com/beto/app/companion/CompanionLlmClient.kt:
```kotlin
interface CompanionChatClient {
    suspend fun chat(history: List<CompanionMessage>): String
    suspend fun extractProfileFact(userText: String): ProfileFact?
}
```

Constructor shapes that callers depend on (no-arg invocations exist in 4 call sites):
- `GeminiLlmClient()` -> will become `ClaudeLlmClient()` (keep no-arg default working)
- `CompanionLlmClient()` -> name unchanged, no-arg default keeps working
</interfaces>

<call_sites>
<!-- Verified via grep. These 4 lines must keep compiling after rename. -->

- android/app/src/main/java/com/beto/app/BetoApplication.kt:39 → `PhraseGenerator(GeminiLlmClient())`
- android/app/src/main/java/com/beto/app/voice/VoiceCaptureActivity.kt:36 → `SttCorrector(GeminiLlmClient())`
- android/app/src/main/java/com/beto/app/service/BetoForegroundService.kt:77 → `llm = GeminiLlmClient(),`
- android/app/src/main/java/com/beto/app/companion/CompanionActivity.kt:50 → `llm = CompanionLlmClient(),` (no rename needed)
</call_sites>

<anthropic_sdk_usage>
<!-- Reference shape — adjust to actual 2.30.0 API. The SDK uses a builder pattern. -->

```kotlin
import com.anthropic.client.okhttp.AnthropicOkHttpClient
import com.anthropic.models.messages.MessageCreateParams
import com.anthropic.models.messages.Model

val client = AnthropicOkHttpClient.builder()
    .apiKey(BuildConfig.ANTHROPIC_API_KEY)
    .build()

val params = MessageCreateParams.builder()
    .model(Model.CLAUDE_HAIKU_4_5)   // o el string "claude-haiku-4-5"
    .maxTokens(512)
    .temperature(0.0)
    .system("...")          // opcional — system prompt
    .addUserMessage(prompt) // user turn
    .build()

val response = client.messages().create(params)
val text = response.content().firstOrNull()?.text()?.text().orEmpty()
```

Notas:
- `client.messages().create(params)` ES BLOCKING — siempre envolver en `withContext(Dispatchers.IO) { ... }`.
- El response es `Message` con `.content()` = `List<ContentBlock>`. El primer block tipo `text` trae el string.
- No hay `responseMimeType = "application/json"` en Claude. El JSON-only se garantiza en el system prompt (`PromptBuilder` ya lo hace) + el retry loop existente en `GeminiLlmClient` ya tolera JSON malformado. Mantener ese retry loop.
- Si el shape exacto del SDK 2.30.0 difiere, consultar https://github.com/anthropics/anthropic-sdk-java README. NO inventar métodos.
</anthropic_sdk_usage>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cambiar dependencias Gradle — remover Firebase, agregar anthropic-java, exponer BuildConfig.ANTHROPIC_API_KEY</name>
  <files>
    android/gradle/libs.versions.toml,
    android/build.gradle.kts,
    android/app/build.gradle.kts,
    android/app/google-services.json
  </files>
  <action>
    1. **`android/gradle/libs.versions.toml`:**
       - Remover `firebaseBom`, `googleServices` de `[versions]`.
       - Remover `firebase-bom`, `firebase-ai` de `[libraries]`.
       - Remover el plugin `google-services` de `[plugins]`.
       - Agregar versión `anthropic = "2.30.0"`.
       - Agregar libraries:
         ```toml
         anthropic-java = { group = "com.anthropic", name = "anthropic-java", version.ref = "anthropic" }
         anthropic-java-client-okhttp = { group = "com.anthropic", name = "anthropic-java-client-okhttp", version.ref = "anthropic" }
         ```

    2. **`android/build.gradle.kts` (root):** Remover la línea `alias(libs.plugins.google.services) apply false`.

    3. **`android/app/build.gradle.kts`:**
       - Remover `alias(libs.plugins.google.services)` del bloque `plugins { }`.
       - En `dependencies { }` reemplazar las dos líneas de Firebase:
         ```kotlin
         // BEFORE
         implementation(platform(libs.firebase.bom))
         implementation(libs.firebase.ai)
         // AFTER
         implementation(libs.anthropic.java)
         implementation(libs.anthropic.java.client.okhttp)
         ```
       - En `defaultConfig { }` agregar carga de la key desde `local.properties`:
         ```kotlin
         val localProps = java.util.Properties().apply {
             val f = rootProject.file("local.properties")
             if (f.exists()) f.inputStream().use { load(it) }
         }
         val anthropicKey = localProps.getProperty("anthropic.api.key", "")
         buildConfigField("String", "ANTHROPIC_API_KEY", "\"$anthropicKey\"")
         ```
       - `buildFeatures { buildConfig = true }` ya está activo — no tocar.

    4. **Eliminar `android/app/google-services.json`** (ya no se usa). Confirmar que `.gitignore` ya tiene `android/app/google-services.json` (sí lo tiene — línea verificada en gitignore root). Eliminar físicamente el archivo del working tree con `rm android/app/google-services.json`.

    5. **`local.properties`:** Agregar (manualmente por el dev — NO commitear) la línea `anthropic.api.key=sk-ant-...`. Si el archivo no existe, crearlo. Confirmar que `android/local.properties` está en `.gitignore` (sí — línea verificada).

    Razón: por D-stack del proyecto (CLAUDE.md "Stack Patterns by Variant" → camino fallback), el SDK oficial es `anthropic-java` 2.30.0 con transport OkHttp. La key embebida via BuildConfig es aceptable explícitamente para demo single-device.
  </action>
  <verify>
    <automated>cd android && ./gradlew :app:dependencies --configuration debugCompileClasspath 2>&1 | grep -E "anthropic|firebase" | head -20</automated>

    Expected: lines with `com.anthropic:anthropic-java`, `com.anthropic:anthropic-java-client-okhttp`. NO lines with `com.google.firebase:firebase-ai` or `firebase-bom`.

    Adicional manual: `ls android/app/google-services.json` debe devolver "No such file or directory".
  </verify>
  <done>
    Gradle resuelve `com.anthropic:anthropic-java:2.30.0` y `:anthropic-java-client-okhttp:2.30.0`. Ninguna dependencia Firebase aparece en el classpath. `google-services.json` no existe. `BuildConfig.ANTHROPIC_API_KEY` es accesible desde Kotlin source (compila — se valida en Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementar ClaudeLlmClient + AnthropicClientHolder, migrar CompanionLlmClient, actualizar call sites</name>
  <files>
    android/app/src/main/java/com/beto/app/llm/AnthropicClientHolder.kt,
    android/app/src/main/java/com/beto/app/llm/ClaudeLlmClient.kt,
    android/app/src/main/java/com/beto/app/llm/GeminiLlmClient.kt,
    android/app/src/main/java/com/beto/app/companion/CompanionLlmClient.kt,
    android/app/src/main/java/com/beto/app/BetoApplication.kt,
    android/app/src/main/java/com/beto/app/voice/VoiceCaptureActivity.kt,
    android/app/src/main/java/com/beto/app/service/BetoForegroundService.kt
  </files>
  <action>
    1. **Crear `android/app/src/main/java/com/beto/app/llm/AnthropicClientHolder.kt`:**

       Singleton thread-safe que expone una helper `suspend fun complete(...)` corriendo en `Dispatchers.IO`. Lazy-init para no penalizar startup si el cliente no se usa todavía.

       ```kotlin
       package com.beto.app.llm

       import com.anthropic.client.AnthropicClient
       import com.anthropic.client.okhttp.AnthropicOkHttpClient
       import com.anthropic.models.messages.MessageCreateParams
       import com.beto.app.BuildConfig
       import com.beto.app.util.LogTags
       import kotlinx.coroutines.Dispatchers
       import kotlinx.coroutines.withContext
       import timber.log.Timber

       /**
        * Holder único del cliente Anthropic. La SDK java es blocking — toda llamada
        * sale en Dispatchers.IO. Modelo fijo: claude-haiku-4-5 (sub-2s, vision + tools).
        */
       object AnthropicClientHolder {
           const val MODEL = "claude-haiku-4-5"

           private val client: AnthropicClient by lazy {
               AnthropicOkHttpClient.builder()
                   .apiKey(BuildConfig.ANTHROPIC_API_KEY)
                   .build()
           }

           /**
            * Llamada single-turn. `system` opcional. Devuelve el primer ContentBlock tipo text.
            * Si el SDK responde algo no-text o vacío, devuelve string vacío y loggea.
            */
           suspend fun complete(
               prompt: String,
               system: String? = null,
               maxTokens: Long = 512,
               temperature: Double = 0.0,
           ): String = withContext(Dispatchers.IO) {
               val params = MessageCreateParams.builder()
                   .model(MODEL)
                   .maxTokens(maxTokens)
                   .temperature(temperature)
                   .apply { if (!system.isNullOrBlank()) system(system) }
                   .addUserMessage(prompt)
                   .build()

               runCatching { client.messages().create(params) }
                   .map { msg ->
                       msg.content()
                           .firstOrNull { it.text().isPresent }
                           ?.text()
                           ?.get()
                           ?.text()
                           .orEmpty()
                   }
                   .getOrElse {
                       Timber.tag(LogTags.LLM).w(it, "ANTHROPIC_CALL_FAILED")
                       ""
                   }
           }
       }
       ```

       Notas de implementación:
       - El shape exacto de `MessageCreateParams.builder()` y `client.messages().create(...)` es el del SDK 2.30.0 documentado en https://github.com/anthropics/anthropic-sdk-java. Si una API diverge (ej. nombre de método para `system` o forma de extraer text del content block), AJUSTAR según el README del SDK — NO inventar.
       - `temperature` es `Double` en el SDK Java. Si fluctúa entre `Double` y `Float`, usar lo que el builder acepte.
       - El primer ContentBlock con text presente alcanza para nuestros prompts (single response).

    2. **Crear `android/app/src/main/java/com/beto/app/llm/ClaudeLlmClient.kt`:**

       Reemplazo 1:1 de `GeminiLlmClient` con la misma estructura (cache, retry para JSON malformado, parseDecision). Conserva el constructor `(cache, generateContent)` para que tests futuros puedan inyectar fakes — el `defaultGenerator()` ahora delega en `AnthropicClientHolder`.

       ```kotlin
       package com.beto.app.llm

       import com.beto.app.util.LogTags
       import com.beto.app.voice.TranscriptCorrectionClient
       import timber.log.Timber

       class ClaudeLlmClient(
           private val cache: LlmCache = LlmCache(),
           private val generateContent: suspend (String) -> String = defaultGenerator(),
       ) : LlmClient, TranscriptCorrectionClient {

           override suspend fun interpret(rawTranscript: String): Decision {
               val sanitized = Sanitizer.sanitize(rawTranscript)
               cache.get(sanitized)?.let {
                   Timber.tag(LogTags.LLM).d("LLM_CACHE_HIT")
                   return it
               }

               val prompt = PromptBuilder.buildInterpretPrompt(sanitized)
               val response = runCatching { generateContent(prompt) }
                   .getOrElse { error ->
                       Timber.tag(LogTags.LLM).w(error, "LLM_GENERATE_FAILED")
                       return Decision.Unknown
                   }

               val decision = parseDecision(response)
                   ?: parseDecision(retryPrompt(prompt, response))
                   ?: Decision.Unknown

               cache.put(sanitized, decision)
               return decision
           }

           override suspend fun correctTranscript(prompt: String): String =
               runCatching { generateContent(prompt) }
                   .getOrElse {
                       Timber.tag(LogTags.LLM).w(it, "LLM_STT_CORRECTION_FAILED")
                       """{"corrected":""}"""
                   }

           override suspend fun generatePhrase(prompt: String): String =
               runCatching { generateContent(prompt) }
                   .getOrElse {
                       Timber.tag(LogTags.LLM).w(it, "LLM_PHRASE_GEN_FAILED")
                       ""
                   }

           private fun parseDecision(raw: String): Decision? = DecisionJson.decode(raw)

           private suspend fun retryPrompt(prompt: String, malformedResponse: String): String =
               runCatching {
                   generateContent(
                       """
                       $prompt

                       La respuesta anterior fue JSON inválido o contenía una tool no permitida:
                       $malformedResponse

                       Respondé de nuevo solo con JSON válido y una tool permitida.
                       """.trimIndent(),
                   )
               }.getOrDefault("")

           companion object {
               private fun defaultGenerator(): suspend (String) -> String = { prompt ->
                   AnthropicClientHolder.complete(
                       prompt = prompt,
                       maxTokens = 512,
                       temperature = 0.0,
                   )
               }
           }
       }
       ```

    3. **Borrar `android/app/src/main/java/com/beto/app/llm/GeminiLlmClient.kt`** completamente. Toda referencia a `Firebase`, `GenerativeModel`, `GenerativeBackend`, `generationConfig` debe desaparecer del repo.

    4. **Reescribir `android/app/src/main/java/com/beto/app/companion/CompanionLlmClient.kt`:** Mantener el nombre `CompanionLlmClient` (es provider-neutral y `CompanionActivity.kt:50` lo usa). Cambiar SOLO los `defaultChatGenerator()` y `defaultFactGenerator()` para usar `AnthropicClientHolder`. Eliminar imports de `com.google.firebase.*`.

       ```kotlin
       package com.beto.app.companion

       import com.beto.app.llm.AnthropicClientHolder
       import com.beto.app.llm.PromptBuilder
       import com.beto.app.llm.Sanitizer
       import com.beto.app.util.LogTags
       import kotlinx.serialization.json.Json
       import kotlinx.serialization.json.jsonObject
       import kotlinx.serialization.json.jsonPrimitive
       import timber.log.Timber

       interface CompanionChatClient {
           suspend fun chat(history: List<CompanionMessage>): String
           suspend fun extractProfileFact(userText: String): ProfileFact?
       }

       /**
        * Cliente del Modo Compañero ahora sobre Claude Haiku 4.5.
        *  - Chat: temperature 0.4, maxTokens 200 (tono natural).
        *  - Fact extraction: temperature 0, maxTokens 80 (JSON estricto via system prompt).
        *
        * Sanitizer aplicado a CADA mensaje del historial antes de mandarlo al LLM.
        */
       class CompanionLlmClient(
           private val sanitizer: Sanitizer = Sanitizer,
           private val chatGenerator: suspend (String) -> String = defaultChatGenerator(),
           private val factGenerator: suspend (String) -> String = defaultFactGenerator(),
       ) : CompanionChatClient {

           override suspend fun chat(history: List<CompanionMessage>): String {
               val sanitized = history.map { it.copy(text = sanitizer.sanitize(it.text)) }
               val prompt = PromptBuilder.buildCompanionChat(sanitized)
               return runCatching { chatGenerator(prompt) }
                   .getOrElse {
                       Timber.tag(LogTags.LLM).w(it, "COMPANION_CHAT_FAILED")
                       ""
                   }
                   .ifBlank { "Estoy acá. Contame un poco más." }
           }

           override suspend fun extractProfileFact(userText: String): ProfileFact? {
               val sanitized = sanitizer.sanitize(userText)
               val prompt = PromptBuilder.buildFactExtraction(sanitized)
               val raw = runCatching { factGenerator(prompt) }
                   .getOrElse {
                       Timber.tag(LogTags.LLM).w(it, "COMPANION_FACT_FAILED")
                       ""
                   }
               return parseFact(raw)
           }

           private fun parseFact(raw: String): ProfileFact? = runCatching {
               val obj = Json.parseToJsonElement(raw.trim()).jsonObject
               val factObj = obj["fact"]?.jsonObject ?: return@runCatching null
               val category = factObj["category"]?.jsonPrimitive?.content?.takeIf { it.isNotBlank() }
                   ?: return@runCatching null
               val fact = factObj["fact"]?.jsonPrimitive?.content?.takeIf { it.isNotBlank() }
                   ?: return@runCatching null
               ProfileFact(category = category, fact = fact)
           }.getOrNull()

           companion object {
               private fun defaultChatGenerator(): suspend (String) -> String = { prompt ->
                   AnthropicClientHolder.complete(
                       prompt = prompt,
                       maxTokens = 200,
                       temperature = 0.4,
                   )
               }

               private fun defaultFactGenerator(): suspend (String) -> String = { prompt ->
                   AnthropicClientHolder.complete(
                       prompt = prompt,
                       maxTokens = 80,
                       temperature = 0.0,
                   )
               }
           }
       }
       ```

    5. **Actualizar 3 call sites** (sed seguro, una línea cada uno):
       - `android/app/src/main/java/com/beto/app/BetoApplication.kt`: cambiar import `com.beto.app.llm.GeminiLlmClient` → `com.beto.app.llm.ClaudeLlmClient` y `PhraseGenerator(GeminiLlmClient())` → `PhraseGenerator(ClaudeLlmClient())`.
       - `android/app/src/main/java/com/beto/app/voice/VoiceCaptureActivity.kt`: cambiar import + `SttCorrector(GeminiLlmClient())` → `SttCorrector(ClaudeLlmClient())`.
       - `android/app/src/main/java/com/beto/app/service/BetoForegroundService.kt`: cambiar import + `llm = GeminiLlmClient(),` → `llm = ClaudeLlmClient(),`.

    6. **Verificar manifest:** `grep -i "firebase\|google" android/app/src/main/AndroidManifest.xml` ya devolvió vacío — no hay nada que limpiar.

    Razón:
    - Singleton holder evita reconstruir el `AnthropicOkHttpClient` por request (ya recomendado en `<constraints>`).
    - `withContext(Dispatchers.IO)` cubre el blocking del SDK Java sin contaminar callers que ya están en suspend.
    - La estructura `(cache, generateContent)` se preserva → tests futuros que inyecten un fake `suspend (String) -> String` siguen siendo posibles. Tests existentes en `src/test/java/com/beto/app/llm/` (Sanitizer, PromptBuilder, LlmCache) NO tocan el cliente y no se rompen.
    - `SttCorrectorTest.kt` usa `TranscriptCorrectionClient` directamente — tampoco se rompe.
    - Logs siguen el tag `LogTags.LLM` (alineado con CLAUDE.md `Beto-LLM`).
  </action>
  <verify>
    <automated>cd android && ./gradlew :app:assembleDebug 2>&1 | tail -40</automated>

    Expected: `BUILD SUCCESSFUL`. Sin errores de unresolved reference. Sin imports de `com.google.firebase`.

    Validación adicional:
    ```
    grep -rn "com.google.firebase\|GeminiLlmClient\|firebase.ai" android/app/src/main 2>/dev/null
    ```
    Expected: 0 matches.

    Y los tests existentes:
    ```
    cd android && ./gradlew :app:testDebugUnitTest 2>&1 | tail -10
    ```
    Expected: `BUILD SUCCESSFUL` (tests existentes son agnósticos al provider).
  </verify>
  <done>
    `./gradlew :app:assembleDebug` produce un APK debug. `./gradlew :app:testDebugUnitTest` pasa. Cero referencias a Firebase o GeminiLlmClient en el código fuente. Las llamadas en runtime salen a Anthropic (esto se valida humanamente con un comando de voz en el teléfono — fuera del scope automatizado de este plan).
  </done>
</task>

</tasks>

<verification>
1. **Build limpio:** `cd android && ./gradlew clean :app:assembleDebug` debe pasar.
2. **Sin Firebase residual:** `grep -rn "firebase\|google-services\|GeminiLlmClient" android/app/src android/app/build.gradle.kts android/build.gradle.kts android/gradle/libs.versions.toml` debe devolver 0 matches relevantes (los únicos aceptables son comentarios históricos si quedaran — preferir cero).
3. **Tests:** `cd android && ./gradlew :app:testDebugUnitTest` debe pasar (todos los tests existentes son agnósticos al provider).
4. **Smoke manual (post-instalación, fuera de gradle):** instalar APK, lanzar comando de voz "mandale un WhatsApp a Juan que ya llegué", verificar logs:
   ```
   adb logcat -s "Beto-LLM:D" "Beto-Action:D"
   ```
   Esperado: log `LLM_CACHE_HIT` o respuesta exitosa de Claude, seguido de `ActionRouter` ejecutando `send_whatsapp`.
</verification>

<success_criteria>
- [x] APK debug compila sin Firebase deps.
- [x] Unit tests existentes siguen pasando.
- [x] `ClaudeLlmClient` (motor de acciones) y `CompanionLlmClient` (chat) ambos rutean a `claude-haiku-4-5` via `AnthropicClientHolder`.
- [x] `BuildConfig.ANTHROPIC_API_KEY` lee desde `local.properties` (gitignored).
- [x] `google-services.json` eliminado del working tree.
- [x] Las 3 call sites de `GeminiLlmClient()` actualizadas a `ClaudeLlmClient()`.
- [x] Cero imports de `com.google.firebase.*` en `app/src/main`.
- [x] Logs mantienen tag `LogTags.LLM` (Beto-LLM).
- [x] Comando de voz manual funciona en el teléfono de demo (smoke test post-build, requerido antes de mergear pero no bloquea el plan).
</success_criteria>

<output>
After completion, create `.planning/quick/260510-11o-migrar-de-gemini-firebase-ai-logic-a-cla/260510-11o-SUMMARY.md` documenting:
- Versión exacta de `anthropic-java` resuelta y cualquier ajuste de API que haya sido necesario hacer al shape sugerido en `<anthropic_sdk_usage>` (si el builder difiere de lo planeado).
- Confirmación de que `local.properties` tiene la key (sin pegarla — solo "key cargada OK").
- Resultado del smoke test en el teléfono de demo.
- Cualquier `Pitfall` encontrado que valga la pena documentar para el equipo (ej. timeouts del SDK, latencia observada).
</output>
