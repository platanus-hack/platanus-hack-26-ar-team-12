---
phase: 3
plan: 03-03
title: "Acceso a contactos del sistema + permiso runtime"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [CONTACTS-01, CONTACTS-02, CONTACTS-03]
files_modified:
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/java/com/beto/app/contacts/ContactRepository.kt
  - android/app/src/main/java/com/beto/app/contacts/ContactInfo.kt
  - android/app/src/main/java/com/beto/app/MainActivity.kt
  - android/app/src/main/java/com/beto/app/util/PreflightCheck.kt
  - android/app/src/main/res/values/strings.xml
  - android/app/src/test/java/com/beto/app/contacts/ContactRepositoryTest.kt
---

<objective>
Reemplazar el `DemoContacts.kt` hardcoded por acceso real a la libreta del sistema. `ContactRepository` lee `ContactsContract` (con permiso runtime `READ_CONTACTS`) y resuelve nombres a `ContactInfo(displayName, phoneNumbers, hasWhatsApp, hasEmail)`. Si el usuario rechaza el permiso, Beto cae a `DemoContacts` y avisa por voz que no podrá usar contactos reales en esta sesión.
</objective>

<user_story>
Como adulto mayor, cuando le digo a Beto "llamá a mi hijo Carlos", quiero que Beto encuentre a Carlos en mi libreta del teléfono — no en una lista hardcoded de contactos demo.
</user_story>

<must_haves>
- CONTACTS-01: `READ_CONTACTS` se solicita runtime desde `MainActivity` durante el onboarding. Si rechazado, Beto opera contra `DemoContacts.kt` con aviso TTS *"No tengo acceso a tus contactos. Solo puedo usar los de prueba."*.
- CONTACTS-02: `ContactRepository.resolve(name): List<ContactInfo>` busca por nombre con `ContactsContract.Contacts.CONTENT_FILTER_URI` (fuzzy nativo de Android). Devuelve lista vacía si no encuentra; lista de >1 si hay homónimos.
- CONTACTS-03: `ContactInfo` incluye `phoneNumbers: List<PhoneNumber>` (con tipo: mobile, home, work) y banderas `hasWhatsApp: Boolean` (chequea `mimetype = vnd.android.cursor.item/vnd.com.whatsapp.profile`) y `hasEmail: Boolean`.
- `ContactRepository.findByPhone(phone)` para resolución inversa (cuando el usuario dice un número).
- Permission flow nunca crashea: si el user rechaza, app sigue funcionando.
</must_haves>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| App lee contactos sin consentimiento | high | Permiso runtime con dialog estándar; `ContactRepository.resolve` lanza `SecurityException` si no hay permiso, callers chequean primero. |
| Datos de contactos loguean a Logcat | medium | NUNCA loguear `displayName` ni `phoneNumber` raw — solo `Contact[id=N]` y conteos. |
| Búsqueda fuzzy devuelve match incorrecto (ej. "Pedro" matchea "Pedrito") | medium | Devolver lista; caller (Plan 03-05) decide. Si lista > 1 → preguntar al user. |
| WhatsApp detection falsa (otro app con mimetype custom) | low | `mimetype` exacto de WhatsApp es estándar y bien documentado. Aceptable. |
| Performance lenta en libretas grandes (1000+ contactos) | low | `CONTENT_FILTER_URI` está optimizado por Android. Aceptable para hackathon. |
</threat_model>

<tasks>

<task id="03-03-T01" type="test">
<title>Tests con ContentProvider mockeado</title>
<action>
`ContactRepositoryTest.kt` usando Robolectric o un fake `ContentResolver`:
- `resolves single match`
- `resolves multiple matches for homonyms`
- `returns empty list when no match`
- `falls back to DemoContacts when permission denied` (mock `checkSelfPermission` denied)
- `marks hasWhatsApp true when WhatsApp mimetype present`
- `marks hasWhatsApp false when no WhatsApp mimetype`
- `findByPhone resolves by exact match`

Si Robolectric agrega fricción, escribir tests instrumented (androidTest) en su lugar.
</action>
<verify>Tests fallan por clase no implementada.</verify>
</task>

<task id="03-03-T02" type="implement">
<title>ContactInfo data class</title>
<action>
`contacts/ContactInfo.kt`:

```kotlin
data class ContactInfo(
    val id: Long,
    val displayName: String,
    val phoneNumbers: List<PhoneNumber>,
    val hasWhatsApp: Boolean,
    val hasEmail: Boolean,
)

data class PhoneNumber(
    val raw: String,
    val e164: String,
    val type: PhoneType,
)

enum class PhoneType { MOBILE, HOME, WORK, OTHER }
```

Helper `String.toE164(defaultCountry: String = "AR"): String` que normaliza a `+54...`. Implementación simple por hackathon — usar `PhoneNumberUtils.formatNumberToE164(number, "AR")`.
</action>
<verify>Compila.</verify>
</task>

<task id="03-03-T03" type="implement">
<title>ContactRepository</title>
<action>
`contacts/ContactRepository.kt`:

```kotlin
class ContactRepository(private val context: Context) {

    fun hasPermission(): Boolean = context.checkSelfPermission(
        Manifest.permission.READ_CONTACTS
    ) == PackageManager.PERMISSION_GRANTED

    fun resolve(name: String): List<ContactInfo> {
        if (!hasPermission()) return emptyList()
        val uri = Uri.withAppendedPath(
            ContactsContract.Contacts.CONTENT_FILTER_URI,
            Uri.encode(name)
        )
        // ...query + map a ContactInfo
    }

    fun findByPhone(phone: String): ContactInfo? { /* ... */ }

    private fun loadPhoneNumbers(contactId: Long): List<PhoneNumber> { /* ... */ }
    private fun hasWhatsAppMimetype(contactId: Long): Boolean { /* ... */ }
    private fun hasEmail(contactId: Long): Boolean { /* ... */ }
}
```

Singleton-friendly: `ContactRepository(applicationContext)` desde `BetoApplication`.
</action>
<verify>Tests del Task 03-03-T01 pasan.</verify>
</task>

<task id="03-03-T04" type="implement">
<title>Permission flow en MainActivity</title>
<action>
En `MainActivity`:
- Sumar paso al onboarding: si `!ContactRepository.hasPermission()`, mostrar UI clara que explica por qué Beto necesita contactos: *"Para que Beto pueda llamar o mandar mensajes a tus contactos, necesitamos permiso para leer la libreta. Tus contactos no salen del teléfono."*
- Botón grande "Dar permiso" → `requestPermissions(arrayOf(READ_CONTACTS), REQ_CODE)`.
- Botón secundario "Continuar sin permiso" → modo limitado.
- En `onRequestPermissionsResult`:
  - Si granted: `TtsManager.speak("Listo, ya tengo acceso a tus contactos.")`.
  - Si denied: `TtsManager.speak("Está bien, voy a usar los contactos de prueba.")` y persistir `LIMITED_MODE = true` en SharedPrefs.

Modificar `PreflightCheck.kt` para que **no** considere bloqueante la falta de READ_CONTACTS (degradación elegante).
</action>
<verify>Smoke test en device: rechazar permiso → app sigue funcionando, Beto lo dice. Otorgar permiso → app sigue funcionando. Cerrar y reabrir → no vuelve a pedir si ya está otorgado.</verify>
</task>

<task id="03-03-T05" type="implement">
<title>String resources nuevos</title>
<action>
Sumar a `strings.xml`:
- `contacts_permission_explanation`: texto del onboarding.
- `contacts_permission_granted_voice`: frase TTS al granted.
- `contacts_permission_denied_voice`: frase TTS al denied.
- `contacts_permission_button_grant` / `contacts_permission_button_skip`.

Mantener idioma argentino con voseo en frases TTS.
</action>
<verify>Resources compilan sin warnings.</verify>
</task>

</tasks>

<exit_criteria>
- `ContactRepository.resolve("Carlos")` devuelve los Carlos de la libreta del device de demo.
- `hasWhatsApp` es `true` para contactos con WhatsApp instalado.
- Onboarding pide permiso una vez, no crashea si rechazado.
- Si permiso denied, `DemoContacts` sigue funcionando como fallback.
- Tests de unidad / instrumented pasan.
- `assembleDebug` compila.
</exit_criteria>
