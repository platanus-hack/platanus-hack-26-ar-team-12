<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required on Android 11+ (targetSdk 30+) for SpeechRecognizer to bind to the
         system RecognitionService. Without this, createSpeechRecognizer() returns a
         recognizer that silently fails (mic activates, no transcript). -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <!-- Visibilidad de paquetes para Quick Actions (Android 11+ scoped package visibility).
             Sin estas entries, getLaunchIntentForPackage devuelve null y caemos al Play Store. -->
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <package android:name="com.google.android.deskclock" />
        <package android:name="com.android.deskclock" />
        <package android:name="com.android.contacts" />
        <package android:name="com.google.android.dialer" />
        <package android:name="com.android.dialer" />
        <package android:name="com.google.android.apps.maps" />
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.CALL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SHOW_ALARMS" />
        </intent>
    </queries>

    <!-- Permisos críticos Phase 1+ (CONTEXT.md + Pitfall #11) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".BetoApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:supportsRtl="true"
        tools:targetApi="34">

        <!-- MainActivity minimalista — pre-flight check launcher (D-09) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".voice.VoiceCaptureActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/Theme.Beto.Transparent" />

        <!-- Modo Compañero (Phase 4-03) — long-press en la burbuja → CompanionActivity -->
        <activity
            android:name=".companion.CompanionActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Beto.Transparent" />

        <!-- Settings de voz — accesible desde el onboarding -->
        <activity
            android:name=".onboarding.VoiceSettingsActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <!-- Configuración del contacto de confianza para el Escudo Antiestafas (Phase 5) -->
        <activity
            android:name=".trust.TrustedContactActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <!-- BetoForegroundService (Plan 03 lo implementa) — Pitfall #11 -->
        <service
            android:name=".service.BetoForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- BetoAccessibilityService (Plan 02 lo crea esqueleto, Phase 4 lo expande) -->
        <service
            android:name=".service.BetoAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
